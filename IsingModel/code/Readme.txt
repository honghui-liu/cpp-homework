(1) there are two .cpp files in this directory, and 2 compilable files (.out), you can run them by (./filename.out),note that the ising_loop file has a for loop and the output will be write to .txt files, so it will take a time to finish. The ising.out file's output will be print on screen.

(2) type ./ising.out -h to get the help information
