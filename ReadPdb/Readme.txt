(1) there is a makefile in this dir, you can just run "make" with "make clean", then there will be a readfile.out file which is compilable;

(2) run readfile.out file (./readfile.out), you will get a "tmpt.pdb" file which is not needed and a out.pdb file which is what we want.
