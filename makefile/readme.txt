(1) the "makefile" file is in this directory which can be executed by command "make", i have already done this, so , there are two executable files in this directory named "rwtime.out" and "rwdist.out", if you run these two files you will get two ".txt" files respectively.

(2) after run "make" command, you can run "make clean" command to remove all the ".o" files, and "make cleanall" to remove all the ".o" and ".out" files.
